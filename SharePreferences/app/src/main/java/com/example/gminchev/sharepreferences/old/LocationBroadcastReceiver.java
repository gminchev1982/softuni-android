package com.example.gminchev.sharepreferences.old;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import android.content.SharedPreferences;
import android.preference.PreferenceManager;

/**
 * Created by GMinchev on 27.3.2018 г..
 */

public class LocationBroadcastReceiver extends BroadcastReceiver {
    @Override
    public void onReceive(Context context, Intent intent) {

        /*SharedPreferences preferences = PreferenceManager.getDefaultSharedPreferences(context);
        SharedPreferences.Editor editor = preferences.edit();
        // editor.putString(ShareKeyLongitude, String.valueOf(longitude));
        editor.putString(LocationListener.ShareKeyLatitude, String.valueOf(latitude));
        editor.commit();*/



    }
}
